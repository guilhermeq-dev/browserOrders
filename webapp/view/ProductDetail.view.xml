<mvc:View
		controllerName="com.lab2dev.browserorders.controller.ProductDetail"
		xmlns:mvc="sap.ui.core.mvc"
		xmlns="sap.m"
		xmlns:f="sap.f"
		xmlns:layout="sap.ui.layout"
		xmlns:form="sap.ui.layout.form"
		height="100%">
		<f:DynamicPage id="dynamicPageId" headerExpanded="{/headerExpanded}" toggleHeaderOnTitleClick="{/titleClickable}">
			<!-- DynamicPage Title -->
			<f:title>
				<f:DynamicPageTitle>
					<f:heading>
						<Title text="Order {/OrderID}"/>
					</f:heading>
					
					
					<f:content>
						<OverflowToolbar>
							
						</OverflowToolbar>
					</f:content>
	
					<f:navigationActions>
						
						<Button
								icon="sap-icon://decline"
								type="Transparent" />
					</f:navigationActions>
				</f:DynamicPageTitle>
			</f:title>
			<!-- DynamicPage Header -->
			<f:header>
				<f:DynamicPageHeader pinnable="true">
					<layout:HorizontalLayout allowWrapping="true">
						<layout:VerticalLayout class="sapUiMediumMarginEnd">
							<ObjectAttribute title="Customer" text="{/Customer/CompanyName}"/>
							<ObjectAttribute title="Ordered" text="23L"/>
							<ObjectAttribute title="Shipped" text="34"/>
						</layout:VerticalLayout>

						<layout:VerticalLayout>
							<ObjectAttribute title="Price"/>
							<ObjectStatus text="In Stock" state="Success" />
						</layout:VerticalLayout>
					</layout:HorizontalLayout>
				</f:DynamicPageHeader>
			</f:header>
			<f:content>
			<layout:VerticalLayout
				enabled="true"
				width="">
			
			<IconTabBar
					id="idIconTabBarMulti"
					expanded="{device>/isNoPhone}"
					class="sapUiResponsiveContentPadding">
				<items>
					<IconTabFilter icon="sap-icon://shipping-status" key="notes">
					<VBox class="sapUiSmallMargin">
							<form:SimpleForm id="SimpleFormDisplay353"
									editable="false"
									layout="ResponsiveGridLayout"
									title="Shipping Address">
							<form:content>
								<Label text="Name" />
								<Text id="nameTextt" text="{SupplierName}" />
								<Label text="Street" />
								<Text text="{Street} {HouseNumber}" />
								<Label text="ZIP Code / City" />
								<Text text="{ZIPCode} {City}" />
								<Label text="Region" />
								<Text id="countryTextt" text="{Country}" />
								<Label text="Country" />
								<Text id="countryTexttt" text="{Country}" />
							</form:content>
							</form:SimpleForm>
						</VBox>
						
					</IconTabFilter>
					<IconTabFilter icon="sap-icon://employee" key="people">
						<VBox class="sapUiSmallMargin">
							<form:SimpleForm id="SimpleFormDisplay354"
									editable="false"
									layout="ResponsiveGridLayout"
									title="Processor Information">
							<form:content>
								<Label text="Name" />
								<Text id="nameText" text="{SupplierName}" />
								<Label text="Employee ID" />
								<Text text="{Street} {HouseNumber}" />
								<Label text="Job Title" />
								<Text text="{ZIPCode} {City}" />
								<Label text="Phone" />
								<Text id="countryText" text="{Country}" />
							</form:content>
							</form:SimpleForm>
						</VBox>
					</IconTabFilter>
				</items>
			</IconTabBar>
			
			
				
			
		
		
			
				<Table id="idProductsTable"
						sticky="HeaderToolbar,ColumnHeaders"
						inset="false"
						items="{
							path: '/ProductCollection',
							sorter: {
								path: 'Name'
							}
						}"
					   class="sapFDynamicPageAlignContent"
					   width="auto">
					<headerToolbar>
						<Toolbar>
							<Title text="Line Items" level="H2"/>
						</Toolbar>
					</headerToolbar>
					<columns>
						<Column
								width="12em">
							<Text text="Product" />
						</Column>
						<Column
								minScreenWidth="Tablet"
								demandPopin="true">
							<Text text="Unit Price" />
						</Column>
						<Column
								minScreenWidth="Tablet"
								demandPopin="true"
								hAlign="End">
							<Text text="Quantity" />
						</Column>
						<Column
								hAlign="End">
							<Text text="Total" />
						</Column>
					</columns>
					<items>
						<ColumnListItem>
							<cells>
								<ObjectIdentifier
										title="{Name}"
										text="{ProductId}"/>
								<Text
										text="{SupplierName}" />
								<Text
										text="{Width} x {Depth} x {Height} {DimUnit}" />
								<ObjectNumber
										number="{
											parts:[{path:'Price'},{path:'CurrencyCode'}],
											type: 'sap.ui.model.type.Currency',
											formatOptions: {showMeasure: false}
										}"
										unit="{CurrencyCode}" />
							</cells>
						</ColumnListItem>
					</items>
				</Table>
			</layout:VerticalLayout>
			</f:content>
		</f:DynamicPage>
</mvc:View>